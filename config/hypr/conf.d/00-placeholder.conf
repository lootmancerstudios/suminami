# Sourced by hyprland.conf via conf.d/*.conf.
#
# This file exists so the glob always matches: Hyprland treats a source= that
# matches nothing as a config error. Generated fragments (web-app window rules)
# and any drop-ins you add land beside it and are git-ignored.
